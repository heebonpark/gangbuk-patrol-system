
# 차량관리 도구(vehicle-management.html)는 2026-09-15부터 시드 데이터 자체가 항상
# 마스킹되어 있어(실명 → 이O호 등) 이 저장소에 커밋해도 안전하다 — 더 이상 gitignore
# 하지 않는다. 회사 실제 데이터는 "엑셀 업로드" 기능으로 그 자리에서 직접 올리면
# 이 파일(소스코드)이 아니라 각자 브라우저의 localStorage에만 저장된다. 실제 데이터가
# 든 xlsx 원본이나, 과거에 실수로 만들어졌던 마스킹 안 된 사본은 여기 커밋하지 말 것.
